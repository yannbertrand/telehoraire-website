---
import { Image } from "astro:assets";
import src from "./tfx.svg";

interface Props {
	width?: number;
	height?: number;
}

const { width = 80, height = 80 } = Astro.props;
---


  <Image
    src={src}
    alt=""
    width={width}
    height={height}
    class="channel-cover--tfx"
  />


<style>
@media (prefers-color-scheme: dark) {
  .channel-cover--tfx {
    filter: grayscale(20%) saturate(80%) brightness(80%);
  }
}
</style>
